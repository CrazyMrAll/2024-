#include <iostream>
#include <cstring>
using namespace std;

char *plus(char a[], char b[], char ans[]) {
    int signa, signb;
    signa = signb = 1;
    
}

int main()
{
    char a[100], b[100], ans[100];
    cin >> a >> b;
    cout << plus(a,b,ans) << endl;
    return 0;
}