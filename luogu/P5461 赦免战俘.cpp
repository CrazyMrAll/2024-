#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    int n, x, y, i, j;
    scanf("%d", &n);
    x = y = pow(2,n);
    for ( i=0; i<y; i++ ) {
        for ( j=0; j<x; j++ ) {
        }
    }
    return 0;
}